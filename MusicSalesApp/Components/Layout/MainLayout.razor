@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Hosting
@using MusicSalesApp.Components.Shared
@inject IWebHostEnvironment HostEnvironment

<ThemeProvider @rendermode="InteractiveServer" />
<AdminMessageDialogHost @rendermode="InteractiveServer" />

<div class="page">
    <NavMenu @rendermode="InteractiveServer" />
    <MobileAppInstallBanner @rendermode="InteractiveServer" />

    <main>
        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    @if (HostEnvironment.IsDevelopment() || HostEnvironment.IsEnvironment("Test"))
    {
        <span>An unhandled error has occurred.</span>
        @* href="." is the stock template's link, and it resolves against the document's directory:
           on /song/{SongTitle} "Reload" navigates to /song/, which is a 404. The click reloads the
           page it is actually on; the href is only the no-JavaScript fallback. *@
        <a href="/" class="reload" onclick="location.reload(); return false;">Reload</a>
        <span class="dismiss">🗙</span>
    }
</div>
