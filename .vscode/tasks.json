{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "blazor-build",
            "type": "process",
            "command": "dotnet",
            "args": [
                "build",
                "${workspaceFolder}/MusicSalesApp/MusicSalesApp.csproj",
                "--configuration",
                "Debug",
                "--nologo"
            ],
            "problemMatcher": "$msCompile",
            "group": "build",
            "presentation": {
                "reveal": "silent",
                "panel": "dedicated"
            }
        },
        {
            "label": "Publish with davidTest profile",
            "type": "shell",
            "command": "pwsh",
            "windows": {
                "command": "pwsh",
                "args": [
                    "-NoProfile",
                    "-Command",
                    "$project='${workspaceFolder}/MusicSalesApp/MusicSalesApp.csproj'; $profile='${workspaceFolder}/MusicSalesApp/Properties/PublishProfiles/davidtest - Web Deploy.pubxml'; Write-Host '=== Step 1: Applying EF Migrations ===' -ForegroundColor Cyan; dotnet ef database update --project $project --connection 'Server=SQL8011.site4now.net;Database=db_a9f0e0_davidtest;User Id=db_a9f0e0_davidtest_admin;Password=${input:davidTestPassword};TrustServerCertificate=True'; if ($LASTEXITCODE -ne 0) { Write-Host 'Migration failed!' -ForegroundColor Red; exit 1 }; Write-Host '=== Step 2: Publishing to Web Server ===' -ForegroundColor Cyan; dotnet msbuild $project /t:Build,Publish /p:Configuration=Release /p:PublishProfile=\"$profile\" /p:Password=${input:davidTestPassword}; if ($LASTEXITCODE -le 1) { exit 0 }"
                ]
            },
            "osx": {
                "command": "zsh",
                "args": [
                    "-lc",
                    "echo 'These Web Deploy publish profiles are intended for Windows/MSDeploy. Run this task from Windows with pwsh and Visual Studio build tools.' >&2; exit 1"
                ]
            },
            "args": [
                "-NoProfile",
                "-Command",
                "$project='${workspaceFolder}/MusicSalesApp/MusicSalesApp.csproj'; $profile='${workspaceFolder}/MusicSalesApp/Properties/PublishProfiles/davidtest - Web Deploy.pubxml'; Write-Host '=== Step 1: Applying EF Migrations ===' -ForegroundColor Cyan; dotnet ef database update --project $project --connection 'Server=SQL8011.site4now.net;Database=db_a9f0e0_davidtest;User Id=db_a9f0e0_davidtest_admin;Password=${input:davidTestPassword};TrustServerCertificate=True'; if ($LASTEXITCODE -ne 0) { Write-Host 'Migration failed!' -ForegroundColor Red; exit 1 }; Write-Host '=== Step 2: Publishing to Web Server ===' -ForegroundColor Cyan; dotnet msbuild $project /t:Build,Publish /p:Configuration=Release /p:PublishProfile=\"$profile\" /p:Password=${input:davidTestPassword}; if ($LASTEXITCODE -le 1) { exit 0 }"
            ],
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        },
        {
            "label": "Publish with streamtunes profile",
            "type": "shell",
            "command": "pwsh",
            "windows": {
                "command": "pwsh",
                "args": [
                    "-NoProfile",
                    "-Command",
                    "$project='${workspaceFolder}/MusicSalesApp/MusicSalesApp.csproj'; $profile='${workspaceFolder}/MusicSalesApp/Properties/PublishProfiles/streamtunes - Web Deploy.pubxml'; dotnet msbuild $project /t:Build,Publish /p:Configuration=Release /p:PublishProfile=\"$profile\" /p:Password=${input:streamtunesPassword}; if ($LASTEXITCODE -le 1) { exit 0 }"
                ]
            },
            "osx": {
                "command": "zsh",
                "args": [
                    "-lc",
                    "echo 'These Web Deploy publish profiles are intended for Windows/MSDeploy. Run this task from Windows with pwsh and Visual Studio build tools.' >&2; exit 1"
                ]
            },
            "args": [
                "-NoProfile",
                "-Command",
                "$project='${workspaceFolder}/MusicSalesApp/MusicSalesApp.csproj'; $profile='${workspaceFolder}/MusicSalesApp/Properties/PublishProfiles/streamtunes - Web Deploy.pubxml'; dotnet msbuild $project /t:Build,Publish /p:Configuration=Release /p:PublishProfile=\"$profile\" /p:Password=${input:streamtunesPassword}; if ($LASTEXITCODE -le 1) { exit 0 }"
            ],
            "group": "build",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "davidTestPassword",
            "type": "promptString",
            "description": "Enter the Web Deploy password for davidTest profile",
            "password": true
        },
        {
            "id": "streamtunesPassword",
            "type": "promptString",
            "description": "Enter the Web Deploy password for streamtunes profile",
            "password": true
        }
    ]
}
